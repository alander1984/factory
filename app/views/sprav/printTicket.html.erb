<style>
            body > div {
                position: absolute;
                top: 0;
                bottom: 0;
                left: 0;
                right: 0;
                display: flex;
                justify-content: space-around;
                align-items: center;
                flex-wrap: wrap;
            }
        </style>

<script type="text/JavaScript">

var url = document.location.toString() ; //url

	var e_url = '' ; //edited url
	var p = 0 ; //position
	var p2 = 0 ;//position 2
	p = url.indexOf("//") ;
	e_url = url.substring(p+2) ;
	p2 = e_url.indexOf("/") ;
	var root_url = url.substring(0,p+p2+3);
	setTimeout("location.href = '<%= @url_redirect %>'",5000);

</script>

<script type="text/javascript" src="https://code.jquery.com/jquery-2.1.3.js"></script>
<script>
$(document).ready(function(){
    $.ajax({
        url: '<%= @url_print %>',
        method: 'GET',
        //dataType: "script",        
        success: function(response){
            console.log(response);
            //$(document.body).html(response);
            setTimeout("location.href = '<%= @url_redirect %>'",4000);
        },
        error: function(response){
            //console.log(response);
            //$(document.body).html(response);
            $("#mes").html("Печать недоступна");
            //document.getElementByID("mes").value="Печать недоступна";
            setTimeout("location.href = '<%= @url_redirect %>'",4000);
        }
    })
});
</script>

<div>
    <div><h1 id="mes">Идет печать</h1></div>
</div>